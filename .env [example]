# Essential ENVs
TIMEZONE=Europe/Prague
APP_DEBUG=1
INGRESS_HOST_PORT=8000
SPA_HOSTNAME=spa.hace.local
SPA_NGINX_PORT=8000
WEBSERVER_HOSTNAME=www.hace.local
WEBSERVER_NGINX_PORT=8000

# Utility ENVs
ADMINER_HOSTNAME=adminer.hace.local
ADMINER_PORT=8080
REMOTE_REPO_ADDR=host.docker.internal:5000

# Xdebug
PHP_IDE_CONFIG=serverName=www.hace.local
XDEBUG_CONFIG=remote_host=docker.for.mac.localhost
